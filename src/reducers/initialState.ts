import { LOGIN_STATUS } from "../common/const";
import { userPayload } from "./user/type";

export default {
    app: {
        loginStatus: LOGIN_STATUS.LOG_OUT,
    },
}

export const initStateUser: userPayload = {

}